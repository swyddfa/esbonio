name: Automation

on:
  issues:
    types:
    - labeled

jobs:
  issues:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "${{ github.event.payload.issue }}"
        echo "${{ github.event.payload.label }}"
      
