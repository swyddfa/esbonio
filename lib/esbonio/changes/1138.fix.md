The JavaScript code that is injected into Sphinx builds to support sync scrolling no longer attempts to connect to a WebSocket server forever and gives up after 5 attempts.

Also the setup code for sync scrolling is only executed if an address for the WebSocket server is passed to the webpage via the `ws` query parameter.
